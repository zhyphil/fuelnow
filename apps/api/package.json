{
  "name": "@fuel-now/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "db:down": "docker compose --file ../../compose.yaml down",
    "db:migrate": "docker compose --file ../../compose.yaml exec -T db psql --username fuel_now --dbname fuel_now --file /migrations/0001_initial.sql",
    "db:up": "docker compose --file ../../compose.yaml up --detach --wait db",
    "db:verify": "docker compose --file ../../compose.yaml exec -T db psql --username fuel_now --dbname fuel_now --file /migrations/verify-schema.sql",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  }
}
